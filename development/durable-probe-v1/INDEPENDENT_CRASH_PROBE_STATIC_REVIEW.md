# Independent static abrupt-child/checkpoint probe review

Scoped pre-run acceptance for a new synthetic process-exit/reconciliation fixture. Reviewed crash-probe.py SHA256 4989aeb565dc9eb419b295089cda96df577c92beeb73e58f95648c88084ace78 and durable-probe-intent.json. No source edits, local tests, host calls, provider calls or old suite reruns were made by this reviewer. Runtime acceptance remains pending actual terminal evidence.

The only database path is /output/abrupt-child.sqlite; child executable source is /work/crash-probe.py. Other writes stay directly under /output. The script has no HTTP/provider client, production/candidate paths, service modification, registration, watcher call or ownership transition. capacity_ledger and the pinned unchanged planner supply synthetic accounting only. The fixed15:00 observation/decision timestamps are fixture values, not actual host resource observations or source-authenticated evidence.

The child receives a precise child argument and calls os._exit(73) only after the actual ledger commit and before returning its acknowledgement. Parent waits for that child and requires returncode73. It then reconciles by exact reservation/input digest, requires generation1 and existing receipt, verifies launch/redispatch false, and checks that another reserve is refused as DUPLICATE_RESERVATION. This is an actual abrupt process exit test when run, stronger than an injected exception. It does not demonstrate system power-loss, kernel crash, disk persistence loss, browser reboot or production dispatch authority.

Checkpoint chain: starts at 64 zero hex, hashes each canonical record without trailing newline, embeds the prior hash, appends raw record+newline to progress.jsonl with file fsync, and writes/fsyncs checkpoint.next before atomic replacement of checkpoint.json. There are initial creation and reconciliation records, seven30second interval records, then a COMPLETE record only after elapsed monotonic time is at least210seconds. The script does not independently replay the chain or recheck the final database/original receipt after the waiting period; downstream review should do so from retained originals. A terminal missing COMPLETE or truncated final JSONL line is incomplete, not success.

Important scope/conditions communicated before launch:

- There is no directory fsync after first file creation or checkpoint rename. The records are file-fsynced and the checkpoint replacement is atomic while running, but power-loss-durable directory publication is unproved. Call this process-exit reconciliation plus retained checkpoint observation; do not infer full power-loss durability from the PASS label.
- The initial checkpoint append occurs before exclusive ledger creation. A prohibited rerun in the same output directory could append a new zero-parent chain fragment before create fails. The external controller must establish the fresh exclusive output and durable once-only start identity before invoking this source. Do not redispatch after uncertain start. This review does not establish that external guard by source inspection alone.
- Runtime220seconds provides only10seconds beyond seven sleeps. The child timeout allows15seconds, so worst-case completion before220seconds is not guaranteed. A hard deadline safely produces incomplete/failure; retain and reconcile it, never retry or infer completion. Actual fast child completion may fit; only terminal receipts establish it.
- File receipt/stdout exports use ordinary writes without explicit fsync, though the ledger contains the committed original admission receipt. Later readback/hash checks and actual cloud filesystem durability remain distinct. Chain hashes attest consistency, not authenticated cloud custody or source identity.

The intent pins the reviewed ledger844411c17f0f35dfd139ea1f350457d8428b945c98969fdaf04b9f3fad0cbc2f and plannerb3f233b37b13433ae8bee010dcf8d05dfe630614d24caa392556926c12868c8a,256MiB memory and220-second runtime. Those are declarations until the cloud controller's effective limit/source/mount evidence is observed. No Linux cgroup, isolation, source custody, quota or production result is certified here. The original reservation identity and all failure/partial artifacts must remain consumed and preserved.
