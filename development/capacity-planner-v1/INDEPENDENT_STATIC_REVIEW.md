# Independent static capacity planner review

Scoped PASS for pure conservative arithmetic; no persistent ledger or admission authority. Reviewed source SHA256 b3f233b37b13433ae8bee010dcf8d05dfe630614d24caa392556926c12868c8a and test SHA256 8cef12568d846fcb7e6c6cde296aa3bd818ef41e44b31b129dda703c9d5c8580, independently matching local file bytes. This review launched no Python/test processes, cloud worker, provider calls or mutations. Root separately reports eight tests passed on the exact source in a single cloud run (0.002s); this reviewer did not independently retrieve/reexecute that cloud result. REPORT.md/checkpoint.json still describe the earlier untested authoring checkpoint; that is historical evidence, not a claim that the later run failed to occur.

Conservative accounting is correct for the supplied inputs: requested scratch is pages * phases * exactly 512MiB. Every supplied positive charge is included, including other epochs on the same filesystem and COMPLETED_CHARGED/AMBIGUOUS states. A charge is never refunded merely because the associated worker completed, epoch changed, or current free space already reflects its allocation. Double subtraction may cause conservative refusal, as documented. Margin adds to required bytes. Exact-fit arithmetic is POSSIBLE_ARITHMETIC; one-byte deficit refuses. The retained 15-page/two-phase calculation remains 16,106,127,360 bytes. No per-phase reservation was reduced.

Fail-closed edges are explicit: exact integer types reject booleans/floats, negative/oversize values and zero charge/page/phase/age bounds; collections must be a tuple of <=1024 exact Charge instances; duplicate reservation IDs and foreign filesystem IDs refuse; unknown/released states refuse; observed time must be explicit UTC, no later than decision time and within the configured <=60-second age limit. Charge and total-needed sums are bounded to signed-63-bit maximum. Python integer arithmetic cannot wrap into a false fit. Prior-epoch charges are intentionally retained, rather than filtered out.

The eight tests inspect the meaningful accounting/refusal boundaries described above. Static inspection found no blocking arithmetic defect or input path that creates a reservation or grants writer permission: reservation_created, writer_authority, input_authenticated and allocation_enforced are always false. The identity strings are caller labels, not actual device/inode/custody authentication. The function cannot establish that charges are complete, timestamps truthful, free space current, a quota exclusive, or other callers serialized; these are correctly outside its claim.

Nonblocking auditability improvement for a future new version: return pages, phases, per_phase_bytes and max_age_seconds alongside requested_bytes. The current output preserves the arithmetic amount but not the unique multiplicands/freshness policy, so an external receipt must retain the exact request to reproduce those choices. Do not expand this suggestion into an in-place rewrite of the reviewed source.

This is scratch arithmetic only. A zero caller margin is permitted; it does not prove allowance for originals, Python copies, JSON objects, native/SQLite memory, failure archives, filesystem journal overhead, concurrent users or runtime scratch beyond the specified 512MiB phase slots. The output's lack of admission authority is essential. Durable shared accounting, observation identity/custody, exact request retention, atomic reservation, ambiguous-commit recovery, actual memory/disk/time enforcement, original-preserving migration and full finality capacity remain unproven. No campaign/watcher ownership or STATE/HANDOFF files were modified.
