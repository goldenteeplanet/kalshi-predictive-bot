# Registry controller static review

Reviewer `/root/campaign_audit`; local source inspection only. No controller, runner, tests, service, host or provider call executed. No registry or agent-ledger file edited.

Reviewed SHA256 pins: submit `c00f94b5f2b40c5f2591ea8b7e5cdfce583681648a7b0efbbf0b8ae2e4ca89bd`; cutoff `01c234cd661603a9bdf345731843dc9f5c4cd7694ead562757e0b8e915ee5aa3`; runner `0124cc8a2eefce95054492b34e0608ce22f463e7f530ce32d69ae21e19fc22e8`; test runner `db42defa4dcae38b221428906852ae8c1a9d5a9dcb6993284aa5388e24a48798`. Helper pins are deadline_admission `9c121284602552ca872b1368e582a55f2d0c1ad565ecee0126237e755ff8f637`, owned_runtime `7e55fc1994a66cf435c5d0a85270606391571221a2264cb4e8e97ae0d416947d`, worker_entry `f8bbd91debf5d52a7f086bf4d95d6a75de28262e247f28e3fe8cd8836fc38f85`.

Disposition: the proposed fixed-unit, once-only development controller has no blocking structural defect identified within the reviewed trusted-workload scope. **Final dispatch readiness is not established:** `job.input.json` and `test_global_registry.py` did not yet exist in the local directory at this review. The final14:47 UTC not-before,14:53 cutoff, complete source pins and expected-test count therefore remain unverified. Review those final bytes before dispatch; this review does not authorize execution or reuse of any prior attempt.

The exact unit names are alpha-cloud-registry-accounting-v1-20260914 and alpha-cloud-registry-cutoff-v1-20260914. Source, attempt and output directories are separately named under alpha-cloud-dev-20260914. No production-unit wildcard or dynamic target is present. `dispatch_once` exclusively consumes attempt/dispatch-once, preserves intent/input with fsync, refuses before/after the declared window, and marks uncertain submission without retry authorization. The distinct /output/suite directory is also exclusive. An early, failed or uncertain submission consumes its identity; do not clear it or invoke the controller again to compensate.

The cutoff controller is launched and its readiness marker checked before the workload systemd submission. It targets the full exact owned unit with SIGKILL at the earlier of its absolute UTC or computed monotonic deadline. The task itself has RuntimeMaxSec at most60, TimeoutStopSec2, KillMode=control-group, SendSIGKILL and Restart=no. `run_owned` rechecks remaining20-second work allowance plus2-second grace/5-second margin at actual launch, and `worker_entry.py` checks again immediately before exec. The20 seconds are a minimum remaining admission allowance, not a claim that the suite actually needs or runs for20 seconds. No real-time scheduling guarantee follows.

Workload containment requests nobody/nogroup, read-only source at/work, writable dedicated/output, strict system protection, private network/tmp/devices, inaccessible production paths, memory256MiB/no swap, CPU25%,16tasks and low scheduler/I/O priority. The worker checks a subset of effective service properties before tests. Remaining properties, successful namespace mounts, output ownership and writable directory setup require retained effective host evidence. This is reviewed-code development containment, not a hostile-code sandbox or aggregate disk quota. The cutoff controller is privileged but its kill target is a fixed owned unit.

`run_registry_tests.py` discovers only test_global_registry.py and compares suite cardinality to config.expected_tests before invoking unittest.run. The test module is imported during discovery before counting, so final test-source review must establish absence of substantive import-time dispatch or old-fixture execution. A count alone does not attest test identities or meaningful coverage. Source pins must include the runner, test module, registry implementation and all helper imports. The worker trusts its root-owned read-only job config; computing the config's own hash in submit is retained custody, not an independently supplied expected-config pin.

Minor failure behavior: the watchdog marker uses cutoff.isoformat(), whereas submit compares the original config string exactly. Use canonical +00:00 config formatting; a Z spelling mismatch will fail closed after arming the watchdog and consume the attempt. The watchdog's status is represented by its ready marker; final host evidence must confirm that service remains active. Its result may report unit-not-found if the worker already completed; that is not proof of a failed substantive workload by itself. Completion must join the owned worker result, suite exit and controller receipts.

## Final local closure acceptance

The missing-file condition above is now resolved. Fresh read-only hash and AST inspection accepts exact job.input.json SHA `b22c5a3212efb57ca4058001e9bf10006a1f433aaedb0040aac7f1f330dfc616` for the requested prospective14:47–14:53 UTC window. It declares14:43:10 UTC, canonical not_before2026-09-14T14:47:00+00:00 and cutoff2026-09-14T14:53:00+00:00, with expected_tests15. All nine source pins match their current bytes and exact required membership. Registry source is `08e2c891304ebcdd327a047ab022d724424825ec08cabdca0666d12b1e9c26d2`; tests `a6c63eb9119140da312658dfc93fc598b0a52c6be41656fb10e98bcc25e658c4`; final test runner `32f11e8a120f4fa25baa225a1da06cf2b4a46a4bd6554b524669e8e34227762e`. This final runner pin supersedes the earlier runner pin above; its main guard defers work until explicit invocation.

Static AST contains15 distinct test_ methods in RegistryTests. The test module's import-time statements comprise stdlib imports, the registry module import, function/class definitions and a __main__ guard. Registry import initializes bounded constants/sets and function definitions only. Temporary database creation occurs in setUp; no old fixture dispatch or import-time acquisition is present. The contention test starts exactly two spawn children inside its test body, with bounded barrier/join/queue waits and cleanup. It does not launch controller/helper jobs. The fixture timestamps around15:00 are explicitly synthetic accounting inputs, not a request to delay the actual scheduled suite or a claim of real observed storage capacity.

Scoped final static PASS for this exact development bundle; no local execution or real workload acceptance occurred in this review. Earlier containment, actual host-property/readiness evidence and once-only/no-retry requirements remain. Test outcomes must come from the new owned run, not this static case count.
