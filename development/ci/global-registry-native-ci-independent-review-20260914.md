# Scoped native CI script review

Static PASS. New `global-registry-accounting-v1-20260914/run-native-ci-registry.sh` SHA256 `c75fd1ae0e3382241f86abfed72e1b44d223572924763ed86156187c1a0270b6` differs from both retained accepted native-CI copies (SHA `9dbb5ad92c11aef36715fa722349187b49e6e8eb5eccd55bc9d0d037ec7c78c0`) in exactly two source locations: add global-registry-accounting-v1 as the fourth suite; add its expected count15 and required registry_accounting.py/test_global_registry.py files. All other lines are unchanged.

The new suite uses the existing systemd-run wrapper, fixed suite-derived/GitHub-run-derived owned unit,90-second runtime,512MiB memory,50% CPU,32-task cap, private network, source read-only/work and dedicated/output mounts, strict system protection and whole-control-group cleanup/checks. Test discovery and unittest execution occur inside that unit. No new uncontained test command, historical cloud controller invocation, provider acquisition or independent process launcher is introduced. The inherited post-unit Python command only inspects the deadline suite's retained escaped-descendant marker and cgroup absence; it does not run tests.

This is a source-diff review only; no script, suite, service or host was executed. It is not evidence that the new suite has passed in cloud or CI. Publication remains contingent on the separately required new cloud result and normal CI. The existing runner still discovers test_*.py in each suite directory and checks the count, so final publication must preserve the reviewed directory membership; count validation alone is not a source-identity guarantee.
