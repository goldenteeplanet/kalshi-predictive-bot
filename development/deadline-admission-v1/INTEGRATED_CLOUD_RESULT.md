# Integrated deadline admission and whole-cgroup result

September14,2026. Fifteen NEW tests passed in11.150seconds: eight admission fixtures and seven real execution/group-boundary fixtures. No consumed planner, ledger or long probe suite was repeated. The development job itself used the reviewed admission path, fresh effective runtime, actual worker entry gate, owner-only UTC/monotonic monitor and an independent absolute cgroup cutoff armed BEFORE systemd submission.

Actual systemd start14:03:49UTC, parent worker start14:03:50.096889. Absolute cutoff14:04:50. Effective native runtime53seconds, worker runtime52seconds; stop grace2seconds and safety5seconds preserved. Tests and parent cleanup completed14:04:04.135303UTC,14.038seconds after worker start. The older source/input/intent and actual-submission originals are retained under attempt-deadline-v1.

Effective unit receipt verifies Type=exec,ExitType=main,RemainAfterExit=no,KillMode=control-group,SendSIGKILL=yes,TimeoutStopUSec=2s,RuntimeMaxUSec=53s,MemoryMax268435456,CPUQuota250ms/second,TasksMax16,PrivateNetwork=yes,ProtectSystem=strict. Source/production isolation follows the exact reviewed binds; no live/demo/provider/DB mutation. Production remained clean at d689838b6f351944540913b64980c9c6ae621d23.

New cases prove delayed submission/worker-entry refusal before substantive work, uncertain callback identity consumption, actual persistence delay, actual owned-child overrun, leader-exits-first cleanup and TERM-resistant same-group descendant cleanup. The Python monitor keeps the leader unreaped with WNOWAIT, then sends TERM and unconditional KILL to its exact group before reaping adopted descendants. No broad process scan or global kill occurs. Preserved owned_runtime.before-group-review.py records the unexecuted reviewed defect; it was not staged for execution.

The final fixture deliberately created a descendant in a NEW session, outside the Python process group but inside the owned systemd cgroup. Marker records PID1754130 and cgroup/system.slice/alpha-cloud-deadline-v1-20260914.service. After main completion, actual unit status was inactive/dead,MainPID0,Resultsuccess,ExecMainStatus0; the entire owned cgroup was absent. This supplies the outer-boundary evidence that same-group tests alone could not provide.

The controller intentionally raised SIMULATED_ACK_LOSS_AFTER_ACTUAL_SYSTEMD_SUBMISSION after one real daemon submission. dispatch_once retained submission-uncertain.json and consumed the identity. A deliberate second controller request refused IDENTITY_CONSUMED before its callback; no second daemon job or cutoff was launched. This was a new integrated refusal fixture, not a repeated workload. Native unit settings were retained while live rather than inferred after transient collection.

The prearmed absolute cutoff ran at14:04:50.021952; systemctl reported the already-completed unit not loaded. No late job remained to kill. The owned cutoff was reconciled/stopped after evidence. This normal integrated path did not need the earlier probe's emergency cutoff correction.

Limits: caller clock/source authority, global filesystem registry/quota, kernel scheduling guarantees and power-loss durability are not established. Subreaper/process-group cleanup covers its declared cooperative context; whole-cgroup containment is supplied by the actual outer systemd unit. No general hostile-code or uninterruptible-kernel guarantee. Case-specific real execution receipts do not certify arbitrary future required-runtime estimates.

Remaining CI gap: the existing repository safety workflow does not execute these development directories. Auth and upstream base are available through the established local control plane; they are not blockers. Next work is a dedicated focused CI workflow on the reviewed development branch and publication through that mechanism, with actual CI results retained. Keep origin push-disabled against production. Do not call source commit or this cloud job remote CI/deployment.
